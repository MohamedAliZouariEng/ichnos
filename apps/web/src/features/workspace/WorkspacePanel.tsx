// Placeholder until Step 8 part 2 adds the workspace form.
export function WorkspacePanel() {
  return (
    <section aria-labelledby="workspace-title">
      <h1 id="workspace-title">Workspace</h1>
      <p className="lede">Connect the GitHub repository Ichnos should read.</p>
    </section>
  );
}
